/**
 * @repo/ui
 * Shared UI components for web (Next.js) and native (Expo).
 * Add cross-platform components here and export them from this file.
 */

// Placeholder — add shared components here
// Example: export { Button } from './Button';
export const UI_VERSION = '0.0.0';
